import 'package:flutter/material.dart';

class ArticleScreen extends StatelessWidget {
  static const routeName = "/article";

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
      title: Text("BRING THEM TO LINK"),
    ));
  }
}
